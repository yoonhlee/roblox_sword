--!strict
--[[
	DataService — ProfileStore 기반 프로필 로드/저장/마이그레이션. 로드 실패 시 Kick, BindToClose 에서 저장 완료까지 대기.
	(구현 예정: Phase 1)
]]

local DataService = {}
DataService.Name = "DataService"

function DataService.Init() end

function DataService.Start() end

return DataService
