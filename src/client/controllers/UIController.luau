--!strict
--[[
	UIController — 화면 구성과 입력 처리. 중앙 무기, 하단 강화 버튼, 확률표 패널, 반응형(UIScale/UIAspectRatioConstraint) 레이아웃. 요청 중에는 버튼 비활성화.
	(구현 예정: Phase 1~2)
]]

local UIController = {}
UIController.Name = "UIController"

function UIController.Init() end

function UIController.Start() end

return UIController
