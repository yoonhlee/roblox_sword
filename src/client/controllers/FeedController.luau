--!strict
--[[
	FeedController — 결과 피드(채팅 로그처럼 한 줄씩 쌓이는 영역). 대사는 LocalizationTable 의 NPC 문구를 사용한다.
	(구현 예정: Phase 2)
]]

local FeedController = {}
FeedController.Name = "FeedController"

function FeedController.Init() end

function FeedController.Start() end

return FeedController
